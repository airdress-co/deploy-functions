extends: default
rules:
  document-start: disable
  line-length: disable
  # `on:` is a workflow key, not a boolean.
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  braces:
    max-spaces-inside: 1
  brackets:
    max-spaces-inside: 1
